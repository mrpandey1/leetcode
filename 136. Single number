class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        nums.sort()
        i=0
        while(i<len(nums)):
            try:    
                if nums[i]!=nums[i+1]:
                    return nums[i]
                else:
                    i+=1
            except:
                return nums[i]
            i+=1
